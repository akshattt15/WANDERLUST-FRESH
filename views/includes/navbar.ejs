<nav class="navbar navbar-expand-lg bg-white border-bottom sticky-top shadow-sm">
  <div class="container-fluid">
    <!-- Brand -->
    <a class="navbar-brand fs-4 fw-bold" href="/listings">
      <i class="fa-regular fa-compass me-2"></i>Wanderlust
    </a>

    <!-- Toggle Button (for mobile) -->
    <button
      class="navbar-toggler border-0"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Navbar Links -->
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link fw-semibold" href="/listings">EXPLORE</a>
      </div>

      <div class="navbar-nav ms-auto">
        <a class="nav-link fw-semibold" href="/listings/new">NEW LISTING</a>

        <% if (!currUser) { %>
          <a class="nav-link fw-semibold" href="/signup">Sign Up</a>
          <a class="nav-link fw-semibold" href="/login">Log In</a>
        <% } %>

        <% if (currUser) { %>
          <a class="nav-link fw-semibold" href="/logout">Log Out</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>

